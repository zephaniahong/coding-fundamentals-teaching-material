/* Today's topics
1. Functions
  - Purpose
    * Reusability
    * Encapsulation
  - 2 Steps!!!
    * Declare
    * Call
  - Arguments vs Parameters
  - Mental Model
    * Secondary school math functions -> f(x) = x + 5
*/

// f(x) = 5
// No parameters
function giveFive() {
  return 5;
}

// f(x) = x + 3
function addThree(x) {
  return x + 3;
}

var main = function (input) {
  return "hello " + input;
};
